# What is wrong: !Note block declared inside Scenario sub-block instead of entity root. Error displayed: error[ERR010]: Syntax error: Misplaced !Note block. Notes must be declared immediately under entity headers.
Feature Authentication
{
    Rule "User Login"
    {
        Scenario "Login"
        {
            !Note MisplacedNote [ Note inside scenario ]
        }
    }
}
