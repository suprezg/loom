# What is wrong: Decorator placed above 'Invariants' block where decorators are prohibited. Error displayed: error[ERR020]: Syntax error: Misplaced decorator '@storage(...)'. Decorators cannot be placed above 'Invariants', 'Background', '!Note', or '!Diagram' blocks.
Component AuthService
{
    @storage(AppStorage::users)
    MisplacedDecoratedBackground
    Invariants
    {
        "Security rule"
    }
}
